interface:
  display_name: "Firecrawl Build"
  short_description: "Integrate Firecrawl into app code"
  default_prompt: "Use $firecrawl-build to integrate the explicitly selected Firecrawl endpoint into this project."
policy:
  allow_implicit_invocation: false
